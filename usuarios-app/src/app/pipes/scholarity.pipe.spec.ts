import { ScholarityPipe } from './scholarity.pipe';

describe('ScholarityPipe', () => {
  it('create an instance', () => {
    const pipe = new ScholarityPipe();
    expect(pipe).toBeTruthy();
  });
});
